notes for uday man:
maybe we have a "uninstall button" to uninstall all or something.

Xn%66v9q


todo:
list dynamic checks √ 
links to casks repos √ 
gens the script magic ---> uday!?!!

curl -o installrurl installr && curl -o ourgeneratedurl .installr.local && sh installr 2>&1 | tee ~/installr.log

curl -o installr ./installr && curl -o localhost:5000/i/0ad .installr.local && sh installr 2>&1 | tee ~/installr.log


css! 



curl -o installrurl installr && curl -o ourgeneratedurl .installr.local && sh installr 2>&1 | tee ~/installr.log

curl -o installr http://buytheway.netne.net/installr && curl -o .installr.local freeslugs.ngrok.com/i/ammonite && sh installr 2>&1 | tee ~/installr.log

 curl -o http://buytheway.netne.net/installr installr && curl -o freeslugs.ngrok.com/i/ammonite .installr.local && sh installr 2>&1 | tee ~/installr.log


